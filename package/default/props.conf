[vmware:vision:vcenter]
SHOULD_LINEMERGE = false
LINE_BREAKER = ([\r\n]+)(?=(?:<\d{1,3}>)?(?:1\s+)?(?:\d{4}-\d\d-\d\d[T ]|[A-Z][a-z]{2}\s+\d{1,2}\s)|Event\s+\[)
TRUNCATE = 262144
MAX_TIMESTAMP_LOOKAHEAD = 40
TIME_PREFIX = ^(?:<\d+>)?(?:1\s+)?
KV_MODE = none
CHARSET = UTF-8
category = Application
description = VMware Vision normalized syslog events
LOOKUP-vmware_vision = vmware_vision_normalize _raw host sourcetype OUTPUT event_id event_type event_time event_chain_id user vcenter vm_name vm_id vm_uuid vm_key identity_quality datacenter cluster esxi_host datastore action category status observed_state severity message change_details change_fields old_value new_value src_ip task_id parser_status parser_format parser_version event_uid vendor_product record_kind component vm_reference_count vmware_action cim_dataset app src dest dvc signature signature_id reason result change_type object object_id object_category object_attrs command

[vmware:vision:aria]
SHOULD_LINEMERGE = false
LINE_BREAKER = ([\r\n]+)(?=(?:<\d{1,3}>)?(?:1\s+)?(?:\d{4}-\d\d-\d\d[T ]|[A-Z][a-z]{2}\s+\d{1,2}\s)|Event\s+\[)
TRUNCATE = 262144
MAX_TIMESTAMP_LOOKAHEAD = 40
TIME_PREFIX = ^(?:<\d+>)?(?:1\s+)?
KV_MODE = none
CHARSET = UTF-8
category = Application
description = VMware Vision normalized syslog events
LOOKUP-vmware_vision = vmware_vision_normalize _raw host sourcetype OUTPUT event_id event_type event_time event_chain_id user vcenter vm_name vm_id vm_uuid vm_key identity_quality datacenter cluster esxi_host datastore action category status observed_state severity message change_details change_fields old_value new_value src_ip task_id parser_status parser_format parser_version event_uid vendor_product record_kind component vm_reference_count vmware_action cim_dataset app src dest dvc signature signature_id reason result change_type object object_id object_category object_attrs command

[vmware:vision:esxi]
SHOULD_LINEMERGE = false
LINE_BREAKER = ([\r\n]+)(?=(?:<\d{1,3}>)?(?:1\s+)?(?:\d{4}-\d\d-\d\d[T ]|[A-Z][a-z]{2}\s+\d{1,2}\s)|Event\s+\[)
TRUNCATE = 262144
MAX_TIMESTAMP_LOOKAHEAD = 40
TIME_PREFIX = ^(?:<\d+>)?(?:1\s+)?
KV_MODE = none
CHARSET = UTF-8
category = Application
description = VMware Vision normalized syslog events
LOOKUP-vmware_vision = vmware_vision_normalize _raw host sourcetype OUTPUT event_id event_type event_time event_chain_id user vcenter vm_name vm_id vm_uuid vm_key identity_quality datacenter cluster esxi_host datastore action category status observed_state severity message change_details change_fields old_value new_value src_ip task_id parser_status parser_format parser_version event_uid vendor_product record_kind component vm_reference_count vmware_action cim_dataset app src dest dvc signature signature_id reason result change_type object object_id object_category object_attrs command

[vmware:vision:json]
SHOULD_LINEMERGE = false
LINE_BREAKER = ([\r\n]+)
TRUNCATE = 262144
MAX_TIMESTAMP_LOOKAHEAD = 40
TIME_PREFIX = "(?:createdTime|timestamp|time)"\s*:\s*"?
KV_MODE = none
CHARSET = UTF-8
category = Application
description = VMware Vision normalized syslog events
LOOKUP-vmware_vision = vmware_vision_normalize _raw host sourcetype OUTPUT event_id event_type event_time event_chain_id user vcenter vm_name vm_id vm_uuid vm_key identity_quality datacenter cluster esxi_host datastore action category status observed_state severity message change_details change_fields old_value new_value src_ip task_id parser_status parser_format parser_version event_uid vendor_product record_kind component vm_reference_count vmware_action cim_dataset app src dest dvc signature signature_id reason result change_type object object_id object_category object_attrs command
